# Runtime deps only — no torch/ultralytics. The exporter stage adds those
# (requirements-export.txt) for the one-time model export.
opencv-python-headless==4.10.0.84
flask==3.1.0
python-telegram-bot==21.9
onnxruntime==1.20.1
numpy==2.4.6
